# 14:48 — Админка: планировка и спринты (трек А)

> Точка входа для работ по админ-панели. Родился 2026-07-10.
> Контекст: STATUS.md (Admin MVP, спринт 3б), DESIGN.md (язык «14:48 Noir»).
> Правило то же, что у Д-спринтов: не начинаем следующий, пока текущий
> не закрыт и не закоммичен. Галочка ✅ ставится здесь.

---

## Принципы (не обсуждаются внутри спринтов)

1. **Один файл** `web/admin.html`, ванильный JS, без сборки — как shell.html.
   React-скелет `admin/` не трогаем (после пилота решим: удалить или возродить).
2. **Язык Noir**: чёрная база, красный `#ff2740` — единственный акцент,
   волосяные линии `rgba(255,255,255,.07)`, стекло без рамок.
   **Без FX-шоу** (tilt, конфетти, shimmer) — админке нужны плотность и скорость.
   Семантические цвета статусов остаются: зелёный/оранжевый/красный/серый.
3. **Иконки** — Phosphor duotone, второй тон `#ff2740` (механика `data-ic` +
   `mountIcons()` переносится из shell.html). Эмодзи уходят отовсюду, кроме тостов.
4. **Частота = близость**: чем чаще действие, тем меньше кликов до него.
   Иерархия задач админа: зал → деньги → гость → брони → каталог → журнал → owner.
5. **Никаких `prompt()`/`confirm()`**: формы — в правом drawer, опасные
   действия — красная кнопка с повторным нажатием (arm → confirm), тост с undo
   где отмена безопасна.
6. **Адаптив с каркаса**: строим под десктоп за стойкой, но сетки — flex/grid,
   на узком экране rail сжимается в иконки, таблицы схлопываются в карточки.
   Полная мобильная полировка — А7.
7. Цифры — `font-variant-numeric: tabular-nums`. Время — «1ч 20м», деньги — zł.
8. Каждое действие админа со временем попадает в журнал (полный аудит — А4).

---

## Планировка (информационная архитектура)

### Каркас

```
┌──────────────────────────────────────────────────────────┐
│ топбар: лого 14:48 ADMIN · поиск «/» · live-точка · кто   │
├──────────┬────────────────────────────────┬──────────────┤
│ rail     │ контент раздела                │ drawer       │
│ Зал      │                                │ (по клику:   │
│ Гости    │                                │ ПК / гость / │
│ Брони ⑥  │                                │ форма)       │
│ Каталог  │                                │ Esc = закрыть│
│ Журнал   │                                │              │
│ ─────    │                                │              │
│ Статист. │  (owner)                       │              │
│ Настройки│  (owner)                       │              │
│ ─────    │                                │              │
│ админ ⏻  │                                │              │
└──────────┴────────────────────────────────┴──────────────┘
```

- **Левый rail** вместо верхних табов: 5 рабочих разделов + 2 owner-раздела
  за волосяным разделителем; внизу — имя и выход. Бэйдж на «Брони» — число
  броней сегодня. На узком экране rail сжимается до иконок (56px).
- **Топбар**: лого (пульс двоеточия — единственный FX, наш знак), глобальный
  поиск по хоткею `/` (ищет гостя/ПК, ведёт в раздел), индикатор
  «live · обновлено N с назад», имя · роль.
- **Правый drawer** — все детали и формы. Таблицы никогда не ведут на
  отдельные «страницы» и не открывают модалок поверх всего.
- **Тосты** снизу по центру; у отмены брони и разбана — кнопка «Вернуть».
- **Хоткеи**: `1–7` разделы, `/` поиск, `Esc` закрыть drawer/форму.

### Разделы — что где и почему

| # | Раздел | Что внутри | Главные действия |
|---|--------|-----------|------------------|
| 1 | **Зал** (дефолт) | KPI-строка + карта ПК по зонам. Плитка ПК: имя, статус цветом, ник гостя + таймер сессии; «скоро бронь» — глиф часов. Заменяет обе старые вкладки «ПК» и «Сессии» — это один и тот же вопрос «кто где сидит» | drawer ПК: завершить сессию (arm-confirm, показывает что начислится), в ремонт / из ремонта, инфо Shell (online, agent) |
| 2 | **Гости** | Живой поиск (debounce 300мс, без потери фокуса), таблица: ник · LVL · монеты · статус · был активен. Быстрое действие прямо в строке — одно: «Депозит» (самое частое) | drawer гостя: профиль (LVL, XP, ранг, монеты, кейсы) + мини-вкладки Сессии / Депозиты / Начисления; действия: депозит (сумма + метод cash/card/blik), +XP (сумма+причина), кейс (тир кнопками), бан (arm-confirm) / разбан |
| 3 | **Брони** | Фильтр: сегодня / завтра / все + по статусу. Таблица: когда · гость · ПК · длительность · предоплата · статус | отменить (с undo-тостом); создать walk-in бронь за гостя (форма в drawer) |
| 4 | **Каталог** | Таблица как сейчас: id · название · категория · target · вкл | drawer-форма вместо инлайновой; тумблер вкл/выкл; порядок стрелками ↑↓ (drag — опция позже); предпросмотр плитки 2:3 как на гостевом экране |
| 5 | **Журнал** | Единая лента всех действий: гранты, депозиты, баны, форс-энды, отмены броней. Фильтры: тип / админ / гость | только чтение |
| 6 | **Статистика** (owner) | Выручка сегодня/неделя, спарклайн 14 дней, загрузка по часам, топ гостей по часам/депозитам | переход в карточку гостя |
| 7 | **Настройки** (owner) | Экономика: XP/мин, coins/мин, курс zł→монеты, тарифы зон; адрес API | сохранить (сейчас значения захардкожены в бэкенде — см. А5) |

### KPI-строка Зала (кликабельная)

`Выручка сегодня` (после А5 — из deposits) · `В зале N` · `Свободно N/M` ·
`Броней сегодня N` (клик → Брони). Заменяет четыре пассивных чипа в шапке.

### Иконки (Phosphor duotone, все — второй тон #ff2740)

| Место | Иконка (phosphor) |
|-------|-------------------|
| Зал | `monitor` |
| Гости | `users-three` |
| Брони | `calendar-blank` |
| Каталог | `squares-four` |
| Журнал | `scroll` |
| Статистика | `chart-bar` |
| Настройки | `gear-six` |
| Выход | `sign-out` |
| Поиск | `magnifying-glass` |
| Депозит | `coins` |
| +XP | `lightning` |
| Кейс | `gift` (вариант: сундук из game-icons — как на гостевом) |
| Бан / разбан | `prohibit` / `arrow-counter-clockwise` |
| Завершить сессию | `power` |
| Ремонт | `wrench` |
| Отменить бронь | `x-circle` |
| Добавить | `plus-circle` |

### Статусы ПК на карте (цвет + подпись)

| Статус | Вид |
|--------|-----|
| Свободен | нейтральная плитка, зелёная точка, «своб.» |
| Занят | красная заливка 16%, ник + таймер |
| Скоро бронь (< 60 мин) | глиф `clock`, «бронь 19:00» |
| Ремонт (maintenance) | оранжевая рамка, глиф `wrench` |
| Shell offline | пунктирная рамка, серый |

---

## Спринты

Каждый спринт закрывается, когда: работает у стойки (проверено руками),
закоммичен, галочка здесь. Бэкенд-работы помечены **[go]**.

### А0 — Каркас Noir ✅ 2026-07-10 (коммит 430f627)
Ничего нового по функциям — только пересадка на новую планировку.
- [x] и1: скелет — палитра Noir, левый rail, топбар, drawer-контейнер, тосты,
      адаптивная сетка (rail → иконки на узком экране);
- [x] и2: иконочная механика из shell.html (`UI`-карта, `data-ic`,
      `mountIcons()`), все эмодзи заменены (22 иконки: 10 из shell.html +
      12 Phosphor duotone; search/wrench/clock — про запас для А1);
- [x] и3: перенос разделов как есть (Зал = таблица ПК + сессии второй
      таблицей), stats/settings — заглушки только для owner,
      хоткеи 1–7 / `/` / Esc, live-индикатор «обновлено N с назад»;
- [x] и4: гости — живой поиск с debounce 300мс, точечная перерисовка
      (тулбары и инпуты статичны, KPI через textContent — ввод и фокус
      больше не теряются при 7с-обновлении).

### А1 — Зал ✅ 2026-07-10 (коммит e1a35cb)
- [x] и1: карта ПК по зонам (группировка `zone`), плитки со статусами по табл. выше;
- [x] и2: drawer ПК: инфо, завершить сессию (arm-confirm — второй клик в течение
      4с, на кнопке минуты сессии), живой таймер в drawer;
- [x] и3: **[go]** `PATCH /admin/computers/:id/status` (только available ⇄
      maintenance, чистое правило `canSetComputerStatus` + тест), кнопки
      «В ремонт»/«Из ремонта» в drawer;
- [x] и4: KPI-строка кликабельна; таблицы ПК/сессий удалены (всё на карте);
      «скоро бронь» (окно 60 мин) на плитке — `computer_id` добавлен в
      `/admin/bookings`.

### А2 — Гости ✅ 2026-07-10 (коммит f81c185)
- [x] и1: **[go]** `GET /admin/users/:id` — карточка одним заходом: профиль +
      сводка (наиграно, сессий, внесено zł) + последние 20 сессий/депозитов/
      начислений (ники админов в журнале резолвятся);
- [x] и2: drawer-карточка гостя: профиль + мини-вкладки Сессии/Депозиты/
      Начисления; клик по строке таблицы открывает карточку;
- [x] и3: формы вместо prompt: депозит (сумма + сегменты cash/card/blik),
      +XP (причина обязательна), кейс (тир кнопками); в интерфейсе не осталось
      ни одного prompt();
- [x] и4: бан arm-confirm («Точно забанить?»), бан и разбан с undo-тостом
      «Вернуть» (6с); быстрый «Депозит» в строке — единственная кнопка,
      остальное в карточке; общий механизм armButton.

### А3 — Брони и Каталог ✅ 2026-07-10 (коммит afdef61)
- [x] и1: брони — фильтры Сегодня/Завтра/Все дни + Живые/Все статусы,
      отмена без confirm с undo-тостом «Вернуть»;
- [x] и2: **[go]** `POST /admin/bookings` (walk-in: ник + ПК/первый свободный +
      время + длительность сегментами; те же validateBookingTime/bookingOverlaps)
      и `POST /admin/bookings/:id/restore` (undo отмены с перепроверкой
      пересечений); drawer-форма «Новая бронь»;
- [x] и3: каталог — форма в drawer, тумблеры вкл/выкл, порядок ↑↓ (swap sort
      внутри категории), удаление через arm-confirm — confirm() в интерфейсе
      больше нет;
- [x] и4: живой предпросмотр плитки 2:3 в drawer: обложка covers/{id}.jpg
      поверх градиента (fallback — TAG-типографика), название + подпись.

### А4 — Журнал-аудит ✅ 2026-07-10 (коммит d5c63a1, миграция 016)
- [x] и1: **[go]** `GET /admin/audit` — слияние трёх источников по времени
      (admin_grants + deposits + новая `admin_actions`, миграция 016,
      модель + `logAdminAction`, сбой лога не роняет действие);
- [x] и2: UI ленты: сегменты Все/Деньги/Начисления/Баны/Сессии/Брони/ПК/Каталог,
      живой фильтр по нику гостя или админа (по кэшу), относительное время
      («N мин назад», абсолютное — в title);
- [x] и3: в аудит пишутся: бан/разбан, форс-энд (с минутами и начислениями),
      ремонт/возврат ПК, брони за гостя/отмена/возврат, каталог
      (правка/вкл-выкл/удаление). Вкладка «Журнал» теперь единая лента.

### А5 — Owner: статистика и настройки ✅ 2026-07-10 (коммит 1cf6cc0, миграция 017)
- [x] и1: **[go]** `GET /admin/stats/revenue?days=14` (по дням + сегодня/7/14)
      и `GET /admin/stats/load` (часы суток + топ-10 гостей по минутам с
      депозитами) — только чтение из deposits/sessions;
- [x] и2: раздел «Статистика»: KPI выручки, столбики 14 дней и 24 часов
      (ванильный CSS, tooltip с деталями), топ гостей — клик открывает
      карточку;
- [x] и3: **[go]** таблица `settings` (миграция 017, сид дефолтов) +
      `GET/PUT /admin/settings`; движок читает xp_per_min / coins_per_min /
      coins_per_pln через `settingInt64` с дефолтом; `depositCoins` принимает
      курс параметром (тест дополнен), изменения — в аудит (settings_update);
- [x] и4: раздел «Настройки» — форма трёх значений с границами; на бэке
      `ownerMiddleware`: stats и settings закрыты от роли admin.

### А6 — Live ✅ 2026-07-10 (коммит af699e6)
- [x] и1: **[go]** `/ws/admin` поверх hub (access-токен в query, typ+роль
      проверяются вручную): `ServeAdmin`/`AdminBroadcast`, события
      session_start/end (sessions.go), shell_online/offline (из Run хаба),
      deposit, booking create/cancel/restore (включая брони самих гостей);
- [x] и2: живая лента в Зале (правая колонка, кап 30 событий, иконки по типу),
      live-точка в топбаре показывает «live» при открытом WS;
- [x] и3: события → точечное обновление с debounce 300мс (KPI + текущий
      раздел; открытая карточка гостя перезагружается при депозите);
      при живом WS поллинг замедлен до страховочных 30с, при разрыве —
      реконнект каждые 5с и fallback на прежние 7с.

### А7 — Полировка
Код готов 2026-07-10, спринт закрывается после проверки (десктоп + телефон) + коммита.
- [x] и1: телефон (≤640px): rail — нижняя панель с safe-area, таблицы
      разделов — карточки (шапка скрыта, ячейки в flex-обёртке),
      drawer — на всю ширину, лента компактнее;
- [x] и2: пустые состояния-приглашения: зал (seed), гости (с текстом запроса),
      брони («Новая бронь»), каталог («Добавить»), журнал (пустой vs фильтр);
- [x] и3: хвосты: выбранный раздел живёт в localStorage и восстанавливается
      после перезагрузки (owner-разделы не подсовываются admin'у), «нет связи»
      подкрашивается красным, автофокус на логине, aria-label на иконных
      кнопках, title на кнопках каталога. Страница логина в Noir с А0,
      фокус-кольца и prefers-reduced-motion — тоже.

**Трек А0–А7 завершён.** Дальше по желанию: drag-and-drop каталога,
пагинация гостей за пределами топ-100, канал admin_call от Shell в ленту,
мультиклуб. Новые хотелки — новыми спринтами А8+.

### А8 — Конструктор зала (owner)
У каждого клуба своё число ПК и своя расстановка — владелец собирает схему
зала сам. Права: конструктор только для owner (конфигурация клуба, как
Настройки); admin карту видит и работает с ней, но не двигает. Понадобится
делегирование — добавим флаг-настройку позже.
Код готов 2026-07-11, спринт закрывается после миграции 018 + проверки + коммита.
- [x] и1: **[go]** миграция 018 — `computers.pos_x/pos_y` (NULL = вне схемы),
      `clubs.layout_w/layout_h` (дефолт 12×8); owner-эндпоинты:
      `POST /admin/computers`, `PATCH /admin/computers/:id` (имя/зона/позиция/
      clear_pos; имя уникально в клубе, клетка — один ПК, границы схемы),
      `DELETE /admin/computers/:id` — только без сессий и броней (иначе 409
      «оставь в ремонте»), `PATCH /admin/clubs/:id/layout` (сжатие — только
      без ПК за границей); чистая `posInBounds` + тест; всё в аудит
      (pc_create / pc_update / pc_delete / layout_update);
- [x] и2: Зал — позиционная схема: сетка клуба (горизонтальный скролл на
      узких экранах), ПК на координатах со статусами/таймерами/«скоро бронь»,
      пустые клетки — едва заметный пол; «Вне схемы» — рядом; без координат
      вовсе — прежняя группировка по зонам (fallback);
- [x] и3: «Редактор зала» (кнопка только owner): тап по ПК → тап по клетке =
      переставить; «+» на клетке = новый ПК с позицией; повторный тап по ПК —
      drawer-форма (имя, зона, снять со схемы, удалить arm-confirm); размер
      схемы ± (W 4–30, H 3–20); каждое действие сохраняется сразу; во время
      редактирования live/поллинг не перерисовывают схему;
- [x] и4: полировка: тап-тап работает и пальцем, подсказки при пустой схеме
      (owner — «открой редактор», admin — «попроси владельца»), STATUS.md
      обновлён.

---

## Не делаем (осознанно)

- Переезд на React (`admin/`) — не сейчас; один файл — сила пилота.
- Платежи/Stripe в админке — это спринт 6 основного трека.
- Права тоньше admin/owner (смены, кассиры) — после пилота.
- Drag-and-drop сортировка каталога — стрелок хватит, drag как опция в А7+.
