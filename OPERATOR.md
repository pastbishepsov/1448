# Трек Е — Стойка: что может админ

> План админской (операторской) части. Точка входа для всех сессий по работе
> у стойки.
> Создан 2026-08-24 из рукописной схемы основателя (лист «В. / АДМ.»):
> расшифровка, уточняющие вопросы и гэп-анализ против кода — чат «Админ»
> от 24.08.
> Ритм проекта: спринт = серия маленьких завершённых итераций, каждая — коммит.
> Правки shell.html/admin.html/app.html — только по правилам верификации (база
> из `git show HEAD:файл`, запись sandbox-скриптами, контроль хвоста и размера).
> При правке app.html поднимать пару APP_VER + app_version (version-check iOS).
> Правка шелла = ТРИ словаря SH_RU/SH_EN/SH_PL (Г9-и2), правка PWA — STR_RU/
> STR_EN/STR_PL; RU — источник истины, дыры ловит патчер.

## Как читать план

Этапы I–VII — смысловые блоки («что строим и почему»). Спринты Е0–Е6 —
порядок исполнения («как идём»), внутри — итерации с чекбоксами, каждая
итерация — одна завершаемая единица работы под коммит. Канонический порядок =
номера спринтов. Галочку ставим только на реально сделанное и проверенное;
несделанное не замалчиваем, а оставляем открытым — с пометкой, что мешает.

Решения этого трека нумеруются Р1…; ссылки на решения соседних треков пишем
с буквой трека (Г-Р7 = решение №7 трека «Гость»).

---

## Что с листа УЖЕ есть (не переделываем, только достраиваем)

Сверено с кодом 2026-08-24. Эти пункты схемы основателя закрыты:

| Пункт схемы | Где живёт |
|---|---|
| Владелец нанимает работника, вносит в систему | `/admin/staff/hire`, миграция 025 (В3-3) |
| ФИО + данные работника | кадровая карточка, личные данные не в аудит (В3-2) |
| Дата работы + часы работы | шаблоны смен и график (Б11), табель (В3-4, миграция 026) |
| Создаёт логин и пароль | наём заводит аккаунт с ролью admin |
| Несколько админов одновременно | ролей не ограничено, каждый под своим логином |
| Баланс аккаунта, добавить баланс | депозит → кошелёк в грошах (Г0, миграция 031) |
| Поиск гостя | `/admin/users?q=` — пока только по нику (расширяем в Е0) |
| Бронь + отмена | walk-in, отмена, undo, лок под бронь (А3, Г3–Г4) |
| Вкл / Выкл / перезагрузка ПК | `/computers/:id/power` (Б8, миграция 021) |
| Включить комп командой на сервер | WoL через агента-соседа (Б8; железный чек ждёт клуба) |
| Посадить за комп, залогинить гостя без пароля | `/computers/:id/session` (Б8) |
| Новый гость регистрируется сам за ПК | экран входа/регистрации шелла |
| Чат гость ↔ админ | вызов + переписка + колокол (Б2–Б3, миграция 019) |
| «Админ не считает — система сама» | биллинг-тикер (Г1) + конвертер монет (В4-2) |

---

## Решения основателя (2026-08-24, молча не переигрывать)

- **Р1. Окно [Готов!] и 7 минут.** При посадке на ПК всплывает окно; списание
  НЕ идёт, пока гость не нажал «Готов!» — чтобы не платить за дорогу от стойки
  к машине. Если за `ready_wait_min` (деф. 7) не нажал — **списание стартует
  само** (прямой ответ основателя; вариант «снять посадку» отвергнут: ПК уже
  занят, а гость чаще всего просто отвлёкся).
- **Р2. Пакеты времени остаются.** Отдельное действие «положить на аккаунт
  пакет времени (3 ч, 5 ч и т. д.)» — наряду с «добавить баланс». Это не
  синоним пополнения кошелька: пакет продаётся номиналом и живёт минутами.
- **Р3. Цену часа в коины НЕ переводим** (решение 24.08). На листе была ранняя
  идея «стоимость часа в коинсах»; код ушёл дальше и оказался честнее: цена
  часа зоны — в злотых (`zones.rate_pln`), монеты — кэшбек, который гасится
  только временем через конвертер `coins_per_pln_spend` (В4-2, RESEARCH §4).
  Единая «коиновая» цена смешала бы предоплаченные деньги с тающим кэшбеком —
  ровно тот риск, из-за которого в Г-Р1 разделили балансы.
- **Р4. Удалёнка — на чужом софте** (решение 24.08). Свой VNC не пишем:
  в клубе ставится готовый (AnyDesk / RustDesk), система хранит ID машины и
  даёт кнопку запуска из шторки ПК. Дёшево, снимаемо, и не мы отвечаем за
  дыры в протоколе показа экрана.
- **Р5. «Обойти приложение» = выйти в Windows.** Админ может снять гостевую
  оболочку на конкретном ПК, все ограничения приложения при этом спадают.
  Это ПРАВО РОЛИ, а не пароль на машине: сегодня выход из киоска закрыт
  локальным Ctrl+Alt+Shift+Q + паролем из `shell.json` — кто знает комбинацию,
  тот и вышел, и в журнале клуба этого не видно.
- **Р6. Залогинить гостя может только админ, без ввода пароля** — уже сделано
  (Б8), фиксируем как инвариант: гость за ПК пароль не набирает.
- **Р7. Чат — в обе стороны.** Гость ↔ админ (есть) И админ ↔ владелец/
  поддержка (нет). Второй канал нужен: у стойки некому задать вопрос.
- **Р8. Смена начинается по входу в систему.** «Начало работы только тогда,
  когда он зашёл под своим логином/паролем» — вход админа открывает запись
  табеля сам, а не ждёт, пока человек вспомнит про кнопку.
- **Р9. Инструкция при первом входе.** Новый админ получает F.A.Q. с
  подсвечиванием кнопок — обучение внутри интерфейса, а не PDF на стойке.

---

## Этапы (смысловые блоки)

### Этап I — Посадка и старт игры (ядро трека)

Цель: пункт листа «активирует окошко на компьютере, чтобы гость нажал
[Готов!]» становится правдой, и посадка перестаёт быть моментом, с которого
безусловно капает счёт. Подэтапы:

- I.1 Состояние ожидания у сессии: ПК занят, биллинг молчит, XP и активные
  минуты не идут; дедлайн `ready_wait_min` (деф. 7, 0 = выкл).
- I.2 Точка отсчёта денег: нажал — платит с нажатия; не нажал — с дедлайна
  (Р1). `started_at` сдвигается на подтверждённый момент, и всё, что считает
  от него (биллинг, XP, пауза Г2, AFK, ачивки Г5), едет за ним. Дедлайн чужой
  брони (Г3) считается от ПОСАДКИ — иначе ожидание съедало бы чужое время.
- I.3 Гостевое окно: полноэкранный экран «Готов?» с ником, номером ПК и
  обратным отсчётом; до нажатия лаунчер заперт.
- I.4 Вид у стойки: «ждёт гостя, 6:12» на клетке зала, отмена посадки без
  списания (гость не дошёл вовсе), live-событие в ленту.
- I.5 Новый гость: действие «занять ПК под регистрацию» — машина придержана,
  включена и не перехватывается, пока человек заводит аккаунт (сейчас ПК
  числится свободным и его может занять другой).

### Этап II — Гость под рукой (карточка и поиск)

Цель: закрыть строку листа «Смена пароля или любых данных» и «поиск по
телефону / нику / имени». Сегодня админ умеет ровно четыре вещи с гостем —
банить, пополнять, начислять и гасить монеты; забытый пароль лечится SQL.
Подэтапы:

- II.1 Правка данных гостя админом: ник, имя, фамилия, телефон, e-mail.
- II.2 Сброс пароля: временный, одноразовый, с обязательной сменой при входе
  и отзывом старых refresh-токенов.
- II.3 Поиск по нику, телефону и имени — и в глобальном поиске, и в бронях,
  и при посадке (телефон и имя в БД лежат с миграций 001 и 043, но нигде не
  ищутся).
- II.4 GDPR-разрез: в аудит идёт факт правки и список полей, но НЕ значения
  (образец — кадровая карточка В3-2); пароль в журнал не попадает никогда.

### Этап III — Пакеты времени

Цель: Р2. Пакет — не деньги в кошельке, а купленный запас минут: «3 часа за
N zł». Подэтапы:

- III.1 Конструктор пакетов у владельца (имя, минуты, цена, зона, вкл/выкл) —
  по образцу товаров В2.
- III.2 Выдача админом = продажа: нал/карта/BLIK → выручка, кошельком →
  погашение обязательства (Г-Р7, иначе выручка задвоится).
- III.3 Расход в биллинге. Порядок — «что скорее пропадёт, то первым»:
  монеты (тают, В4-3) → минуты пакета (тают по сроку, если он есть) →
  кошелёк (не тает никогда, Г-Р1).
- III.4 Отмена ошибочной выдачи по образцу В4-4: неотсиженный остаток
  забирается, строка остаётся с пометкой; свою и сегодняшнюю — админ, чужую и
  вчерашнюю — владелец.
- III.5 Срок годности пакета (настройка, 0 = бессрочно) с предупреждением
  гостю шиной Б4.
- III.6 Видимость: остаток в шелле и PWA, строка в отчётах, обязательство
  клуба по непроигранным пакетам в «Деньгах».

### Этап IV — Смена = вход в систему

Цель: Р8. Сейчас табель — ручная кнопка: можно залогиниться и не отметиться,
можно отметиться и уйти пить кофе. Подэтапы: IV.1 вход админа открывает
запись табеля автоматически; IV.2 ручная кнопка остаётся как запасной путь;
IV.3 индикатор «на смене с 14:48 / смена не открыта» в топбаре; IV.4 в сводке
смены — расхождение «работал, но смены нет» как сигнал владельцу.

### Этап V — Чужой ПК: удалёнка и выход в Windows

Цель: две строки прав АДМ с листа. Подэтапы: V.1 ID машины в стороннем софте
и кнопка подключения из шторки ПК (Р4), факт подключения в аудит; V.2
серверная команда «снять оболочку на N минут» с возвратом по таймеру, правом
роли и записью в журнал (Р5); V.3 честная таблица в `shell/README`: что
закрыто софтом, что ждёт Shell Launcher (фаза 2, Enterprise).

### Этап VI — Онбординг админа

Цель: Р9. Подэтапы: VI.1 тур по реальным кнопкам при первом входе (Зал →
посадить → пополнить → ответить на вызов → журнал); VI.2 флаг прохождения на
сервере, а не в localStorage — иначе тур повторяется на каждом ПК клуба;
VI.3 раздел F.A.Q. по сценариям стойки с повтором тура.

### Этап VII — Связь со своими

Цель: Р7 — канал админ ↔ владелец поверх существующего чата (Б2–Б3), колокол
и история. Гостевой чат не трогаем.

---

## Соответствие этапов и спринтов

| Этап | Спринт | Зависимости |
|------|--------|-------------|
| II Гость под рукой | Е0 | — (денег не трогает, риск нулевой) |
| I Посадка и [Готов!] | Е1 | трогает биллинг Г1, паузу Г2, дедлайн броней Г3 |
| III Пакеты времени | Е2 | после Е1 — биллинг правим один раз, не дважды |
| IV Смена = вход | Е3 | независим |
| V Удалёнка и Windows | Е4 | нужен ответ владельца про софт (вопрос 5) |
| VI Онбординг | Е5 | лучше после Е0–Е2: тур должен показывать финальные кнопки |
| VII Связь со своими | Е6 | независим |

Канонический порядок: Е0 → Е1 → Е2 → Е3 → Е4 → Е5 → Е6.
Е3 и Е6 — законные «передышки» между тяжёлыми спринтами, их можно вклинить
раньше. Е5 сознательно последний: рисовать обучение по интерфейсу, который
ещё меняется, — работа дважды.

---

## Спринты

### Е0 «Гость под рукой» (этап II)

Почему первым: ежедневная боль стойки, ноль риска для денег, разблокирует
работу по телефону — а звонок с бронью и есть основной вход гостя.

- [x] и1 (2026-08-24, `guests.go`). `PATCH /admin/users/:id`: ник
  (`nicknameSafe` + уникальность), имя, фамилия, телефон (E.164, уникален —
  поле с миграции 001), e-mail. Только к `role=player` и не к себе
  (`targetPlayer` → `canTargetUser`, Б0). Аудит пишет факт и СПИСОК
  изменённых полей, значения — нет (II.4). Миграции не понадобилось: все
  колонки уже есть (001, 043), а валидаторы `nicknameSafe`/`validateName`/
  `validatePhone` переиспользованы из анкеты Г8 — вторых правил для тех же
  полей не заводим. Новое: `validateEmail` (разбор `net/mail` плюс инвариант
  `nicknameSafe` — значения едут в разметку админки, вектор из QA Б9–Б11;
  форма «Имя &lt;адрес&gt;» не принимается) и `guestTakenReason` — при 409
  точный код даётся, только когда уникальное поле в патче одно, иначе
  честное «занято одно из», а не угадывание. Пустая строка ЧИСТИТ поле
  (GDPR, как в PATCH /me), ник пустым быть не может. Гостю летит
  `profile_updated` шиной Б4 — свои данные менял не он; строки клиента
  приедут в и5, до тех пор шелл покажет общий текст ветки default.
  **Решение по роли:** правка отдана `admin`, а не `owner` — это операционка
  у стойки, звонить владельцу из-за опечатки в нике абсурдно; защита не роль,
  а журнал. Захочет строже — `phone`/`email` уносятся в own-группу одной
  строкой в main.go.
- [x] и2 (2026-08-24, `password.go` + миграция 044). Сброс пароля админом:
  `POST /admin/users/:id/password/reset` — временный пароль из `crypto/rand`,
  три группы по 4 символа («hkr7-mp3q-xv2t») из алфавита БЕЗ похожих знаков
  (0/O, 1/l/I, 5/S): его диктуют вслух через стойку, и «ноль или буква о» —
  это второй заход и очередь. Отдаётся ровно в одном ответе; в базе только
  bcrypt-хеш, в аудите — факт («выдан временный пароль»), в логе сервера —
  ничего. Флаг `must_change_password`: пока гость не заменил временный
  своим, он **сам** сессию не начинает (409 `password_change_required`), а
  посадку АДМИНОМ это не блокирует — у стойки человека опознали лично, и это
  надёжнее пароля; проверка поэтому стоит в `handleStartSession`, а не в
  общем `startSessionFor`. Смена — новый `POST /me/password` (его в проекте
  не было вовсе): текущий пароль спрашиваем всегда, включая первый вход по
  временному, иначе чужой человек за незапертым экраном сменит пароль сам.
  **Отзыв старых токенов:** отзыв по `jti` (миграция 010) годится для logout
  и ротации, где токен у нас в руках, но не достаёт refresh'ы гостя, которых
  мы никогда не видели, — а живут они 30 дней. Поэтому `users.tokens_valid_from`:
  токены с `iat` раньше отсечки недействительны. Проверка — ТОЛЬКО в
  `/auth/refresh`, где пользователь и так читается из базы (проверка бана);
  в `authMiddleware` её нет сознательно — он принципиально не ходит в БД, а
  access живёт ≤15 минут (принятый компромисс проекта, STATUS.md).
  **Найдено живым e2e и исправлено:** первый заход округлял отсечку ВНИЗ до
  секунды, и refresh, выданный в ту же секунду, что и сброс, её переживал.
  Теперь округляем ВВЕРХ (секунда сброса умирает целиком), а свежая пара
  после смены подписывается ЭТИМ ЖЕ моментом (`signTokenAt`/`writeAuthAt`) —
  проходит по построению, а не по удаче.
- [x] и3 (2026-08-24, `guests.go` + `handleAdminUsers`). Поиск
  `/admin/users?q=`: ник ИЛИ телефон ИЛИ имя/фамилия. Раньше искали только по
  нику — то есть поиск был бесполезен ровно там, ради чего нужен: гость
  звонит и называет номер, приходит и говорит «я Ковальский», а ник он и
  забыл — за этим и пришёл. Телефон сравниваем цифра к цифре
  (`regexp_replace(phone,'\D','','g')`), поэтому «+48 123-456-789»,
  «123 456 789» и хвост «456789» — один и тот же гость; порог 4 цифры, иначе
  «77» из ника «Гость-77» тянуло бы пол-клуба. Полное имя склеивается через
  COALESCE — без него строка с одним пустым полем схлопывается в NULL, и
  однофамилец не находится, пока не заполнено имя. Запрос экранируется
  (`escapeLike` + явный `ESCAPE`): «50%» — это поиск, а не «показать всех».
  В ответ добавлено поле `matched` (id → «ник» / «телефон» / «имя» /
  «фамилия» / «имя и фамилия»): у стойки три Ковальских в списке бесполезны,
  а «нашёлся по телефону» — уже ответ. Форма `users` не менялась — админка не
  ломается. Индексов по `lower()` НЕ заводили (план их предполагал): `ILIKE
  '%…%'` ими всё равно не пользуется, это seq scan; на масштабе клуба — доли
  миллисекунды, а когда станет много, лечится триграммным индексом (pg_trgm),
  а не переписыванием логики. Пустой `q` ведёт себя как раньше.
- [x] и4 (2026-08-24, `resolveGuest` в `guests.go`). Бронь и посадка по тем
  же ключам: `/admin/bookings` и `/computers/:id/session` принимают новое
  поле `guest` — ник, телефон ИЛИ имя. Несколько совпадений → **409 со
  списком кандидатов** (ник, имя, телефон и чем именно совпал), а не
  молчаливый «первый попавшийся»: посадить не того гостя — значит списать
  деньги с чужого кошелька, поэтому выбор делает человек. Правила поиска
  вынесены в общий `guestSearchCondition`, которым теперь пользуются и
  `/admin/users`, и резолвер: «нашёлся в поиске» и «сажается по этому же
  запросу» разъезжаться не должны — это закреплено отдельной проверкой e2e.
  Точное совпадение бьёт частичное: гость с ником «Ян» садится по запросу
  «Ян», даже когда в клубе есть «Янек» и «Яна», иначе короткие ники стали бы
  неразрешимо неоднозначными; та же логика для точного телефона.
  **Отступление от плана (осознанное):** старое поле `nickname` НЕ переведено
  на резолвер и осталось строгим — так его понимала админка до Е0, и менять
  смысл живого поля молча нельзя: «посадить Гостя» не должно вдруг начать
  спрашивать «которого из Гость-1, Гость-77». Кандидатов показываем не больше
  пяти — больше у стойки не читают, это уже не «выбери», а «ищи заново».
  Резолвер видит только `role=player`: администратор не может оказаться
  кандидатом на посадку.
- и5. Вёрстка. Разбита на три коммитируемых захода: правка admin.html — самая
  рискованная часть спринта (файл на 265 КБ), и один общий патч пришлось бы
  либо принимать целиком, либо целиком откатывать.
  - [x] **и5а** (2026-08-24, admin.html +5 КБ): в карточке гостя появились
    имя, телефон и e-mail — без них поиск по телефону (и3) был слепым:
    нашли гостя, а свериться не с чем. Кнопки «Данные» и «Пароль» в ряду
    действий. Форма правки отправляет ТОЛЬКО изменённые поля: пошли бы все
    пять, и в журнале каждая правка выглядела бы как «поменяли всё сразу»,
    то есть аудит перестал бы отвечать на вопрос «что именно тронули».
    Сброс пароля подтверждается тем же arm-паттерном, что бан и выключение
    ПК (`confirm()` в интерфейсе проекта нет): первый клик взводит, второй
    выполняет. Временный пароль показывается крупно, моноширинно и с
    разрядкой — его диктуют вслух через стойку; кнопка «Скопировать» с
    честным запасным вариантом (на `file://` без HTTPS буфер обмена
    закрыт). Пароль живёт в памяти страницы и стирается при уходе с формы и
    при смене гостя: он не должен остаться на экране, когда у стойки уже
    другой человек.
  - [x] **и5б** (2026-08-24, admin.html +2,7 КБ и правка `guests.go`).
    Подпись «нашёлся по телефону/фамилии» стоит у строки гостя в таблице и в
    глобальном поиске; плейсхолдеры обоих полей и текст пустого ответа
    больше не обещают поиск «по нику». Посадка и walk-in бронь принимают
    любой ключ, при 409 показывают **выбор из кандидатов** прямо в форме —
    ник, имя, телефон и чем совпал. Для этого `api()` научился доносить тело
    ошибки: без него 409 приходил голым текстом и выбирать было не из чего.
    **Сервер тоже тронут:** резолвер принял третий ключ `guest_id` —
    выбранного из списка адресуем по идентичности, а не пересылаем его имя
    на повторный поиск: это значило бы допускать, что второй поиск ответит
    иначе, чем тот, из которого человек выбирал.
  - [x] **и5в** (2026-08-24, shell.html +6 КБ, app.html +5,5 КБ, правка
    `auth.go`/`password.go`/`guests.go`). Планировались только строки, но
    при написании обнаружилась дыра: сброс пароля из и2 выдавал гостю
    временный, а **сменить его было негде** — формы не существовало ни в
    шелле, ни в PWA, а с временным паролем гость ещё и не может начать
    сессию (409). Функция упиралась в тупик, поэтому и5в закрывает его:
    полноэкранный гейт обязательной смены поднимается сам при входе и при
    появлении флага; закрыть его можно только сменой или выходом (в PWA
    намеренно НЕ шторка — шторку смахивают). Несовпадение и короткий пароль
    ловятся на клиенте, свежая пара токенов из ответа кладётся сразу.
    Строки `profile_updated`, `password_reset`, подписи пяти полей и весь
    гейт — по 14 ключей в каждом из шести словарей, паритет EN/PL проверяет
    сам патчер. `APP_VER` и `app_version` манифеста подняты парой до
    `2026-08-24.9`.
    **Сервер:** payload уведомления `profile_updated` теперь несёт СТАБИЛЬНЫЕ
    ключи полей вместо русских подписей из аудита — гость может сидеть на
    английском или польском, и подпись обязан выбрать его клиент, а не
    сервер.
- Критерий и1: ✅ 2026-08-24, `e2e_guest_edit.py` (38 проверок на живом
  Postgres): ник меняется; имя/фамилия/телефон/e-mail разом — с обрезкой
  краёв, нормализацией телефона в E.164 и апострофа в типографский; пустая
  строка кладёт в БД NULL, а не пустоту; занятый ник/телефон/e-mail → 409 со
  своим кодом, два уникальных разом → «одно из», и после отказа гость не
  изменён ни в одном поле; себя и другого админа править нельзя (403), гость
  чужую карточку — тоже, без токена — 401; десять негативных валидаций
  (короткий/длинный/опасный ник, телефон без «+» и с ведущим нулём, e-mail
  без домена и в форме с именем, имя с цифрой, длинная фамилия, пустой
  патч); в аудите три записи на три успешные правки, отказы не пишутся, а
  ЗНАЧЕНИЙ полей в журнале нет ни одного; гостю пришли три `profile_updated`
  с полями в payload, выдача пометила прочитанными; телефон виден в карточке
  у стойки. Юниты: `guests_test.go` — `validateEmail` (5 принимаемых форм,
  12 отказов, длина) и `guestTakenReason` (8 случаев, включая «стирание не
  считаем уникальным полем»). `go vet`, `go build ./...`, `go test ./...` —
  зелёные.
- Критерий и2: ✅ 2026-08-24, `e2e_password.py` (32 проверки на живом
  Postgres): временный пароль выдан в форме для диктовки и без похожих
  символов; старым паролем не войти; refresh, выданный ДО сброса, отозван;
  временный пускает, клиент видит флаг, а свежая пара переживает собственную
  отсечку; с временным паролем гость сам не садится (409), админ его сажает;
  смена отвергает неверный текущий (401), короткий новый и совпадающий со
  старым (400), а на успехе снимает флаг и выдаёт свежую пару; после смены
  прежний refresh мёртв, новый жив, временный пароль не пускает, старт сессии
  разблокирован; себе и коллеге-админу сбросить нельзя, гостю — тоже; в
  аудите факт без пароля, пароля нет ни в журнале, ни в логе сервера, в БД
  bcrypt-хеш; гостю пришло уведомление. Юниты: форма и алфавит временного
  пароля (200 генераций), границы длины, `passwordCutoff` и
  `tokenIssuedBefore` (включая инвариант «пара, подписанная отсечкой, себя не
  отвергает»). Миграция 044 прогнана дважды — идемпотентна. Регрессия и1:
  38/38. `go vet`, `go build ./...`, `go test ./...` — зелёные.
- Критерий и3: ✅ 2026-08-24, `e2e_search.py` (27 проверок на живом
  Postgres, стенд из трёх гостей — два однофамильца и «шумный» ник с
  цифрами): поиск по нику работает как раньше; полный номер находит одного,
  номер с пробелами и через дефисы — того же, хвост номера — тоже, и во всех
  случаях причина «телефон»; «Ковальск» отдаёт обоих однофамильцев, «Анна» —
  одну, «Ян Ковальский» — по полному имени, регистр не важен; гость без
  имени и телефона ищется по нику и НЕ всплывает в чужой выдаче (COALESCE);
  «%», «_» и «%» внутри запроса — обычные символы, а не «показать всех»;
  пустой `q` отдаёт список без `matched`; без токена 401, гостю 403.
  Юниты: `escapeLike`, `phoneDigits` (включая порог и «Гость-77»),
  `guestMatchReason` (11 случаев + приоритет ника над телефоном + пустые
  поля). Регрессии: и1 38/38, и2 32/32. `go vet`, `go build`, `go test ./...`
  — зелёные.
- Критерий и4: ✅ 2026-08-24, `e2e_seat_by.py` (31 проверка на живом
  Postgres): старое поле `nickname` сажает как раньше и по фамилии НЕ ищет;
  посадка по полному номеру и по номеру через дефисы открывает сессию именно
  тому гостю; фамилия, встречающаяся один раз, и полное имя сажают сразу;
  два однофамильца дают 409 с обоими кандидатами, у каждого сказано, чем он
  совпал, телефон в карточке есть — и при 409 НИКОГО не посадили; после
  выбора точный ник сажает нужного; точный ник бьёт частичное совпадение
  имени; «никого не нашли» → 404, пустой запрос → 400, админ кандидатом не
  становится; walk-in бронь по телефону оформляется на владельца номера,
  неоднозначная — 409 со списком, старое поле `nickname` работает,
  несуществующий гость → 404; и отдельный блок «поиск и посадка понимают
  гостя одинаково» — три ключа, каждый и находит, и сажает. Юнит:
  `guestCandidates` (полная карточка, пустые поля не отдаются, пустой
  список). Регрессии: и1 38/38, и2 32/32, и3 27/27. `go vet`, `go build`,
  `go test ./...` — зелёные.
- Критерий и5а: ✅ 2026-08-24, `smoke5a.js` — playwright против ЖИВОГО
  бэкенда, а не мока (16 проверок): шторка открывается, обе кнопки на месте,
  форма правки разворачивается; имя и телефон появляются в карточке после
  сохранения; правка одного поля даёт тост ровно с одним полем (дельта
  работает); первый клик по сбросу ВЗВОДИТ кнопку, второй выдаёт пароль в
  форме `xxxx-xxxx-xxxx`; этим паролем гость реально входит и получает флаг
  обязательной смены, старый пароль мёртв; уход с формы стирает пароль с
  экрана; ошибок скрипта на странице нет. Патч наложен скриптом от базы
  `git show HEAD:web/admin.html`, каждый из восьми якорей обязан встретиться
  ровно один раз; хвост `</html>` и рост размера проверены.
- Критерий и5б: ✅ 2026-08-24, `smoke5b.js` — playwright против живого
  бэкенда (20 проверок): поиск по телефону находит гостя и подписывает
  «телефон», по фамилии — обоих однофамильцев с подписью у каждого, пустой
  ответ подсказывает, по чему ищем; посадка по телефону открывает сессию
  нужному гостю; неоднозначный запрос показывает выбор из двух с именем,
  телефоном и причиной совпадения, и до выбора НИКОГО не сажает; выбранный
  кандидат садится именно он; бронь принимает тот же ключ, показывает тот же
  выбор и оформляет ровно одну; ошибок скрипта нет. Регрессия: все четыре
  серверных набора (128 проверок) прогнаны ДВАЖДЫ подряд — стабильны.
  **Найдено при отладке:** брони, созданные прогонами e2e, копятся, а на
  стенде три ПК — правило Г3 (окно до чужой брони минус буфер) начинало
  честно не пускать садиться, и падал не тот тест, что смотрел. Гигиена
  стенда (гасить будущие брони в начале прогона) добавлена в `smoke5b.js`,
  `e2e_seat_by.py` и `e2e_password.py`.
- Критерий и5в: ✅ 2026-08-24, `smoke5v.js` — шелл и PWA против живого
  бэкенда, ТРИ языка × два клиента (63 проверки): гейт поднимается сам после
  входа временным паролем, заголовок на нужном языке, на английском и
  польском экранах нет ни одного русского слова; несовпадение и короткий
  пароль ловятся на клиенте; после смены гейт закрывается, флаг снят в БД,
  новый пароль работает; подписи полей в уведомлении — на языке гостя, у
  сброса пароля свой текст, а не заглушка; ошибок скрипта нет.
- **Дефект, найденный этим прогоном и исправленный (важный).** После сброса
  пароля вход в ТУ ЖЕ СЕКУНДУ выдавал refresh-токен, мёртвый с рождения:
  гость входил и тут же получал «войди заново». Причина — гранулярность:
  стандартный `iat` меряет секунды, а сброс и вход умещаются в одну, поэтому
  округление отсечки вниз оставляло дыру (и2, первый заход), а вверх —
  убивало честно выданное следом. Решение: токен носит `iat_ms`
  (миллисекунды выдачи), отсечка берётся БЕЗ округления — раньше неё мертво,
  позже живо. Токены, выданные до этой правки, миллисекунд не носят и судятся
  по секундам в пользу безопасности (лишний перелогин дешевле пережившего
  сброс доступа). Юниты: `TestTokenIssuedBeforeMillis` (включая ровно тот
  случай, что ломался) и `TestTokenIssuedBeforeLegacy`; `e2e_password.py`
  прогнан трижды подряд — 32/32.
- Критерий спринта Е0: ✅ **закрыт 2026-08-24**. Итог: 4 серверных набора
  (128 проверок) + 3 браузерных (99 проверок) — 227 зелёных; `go vet`,
  `go build ./...`, `go test ./...` чистые.

### Е1 «Готов!» (этап I)

- [x] **и1+и2** (2026-08-24, `ready.go` + миграция 045). Итерации слиты в
  одну: и1 без и2 оставляет сессию в состоянии, из которого нет выхода —
  ожидание заведено, а нажать [Готов!] нечем. Поля `ready_at`,
  `ready_deadline` на сессии; статус НЕ трогаем (остаётся `active`) — тот же
  приём, что у паузы Г2-и1, иначе пришлось бы переписывать десятки выборок
  по enum. Настройка `ready_wait_min` (деф. 7, границы 0–30, 0 = окно
  выключено) заведена сразу в трёх местах — миграция, `settingBounds`,
  `currentSettings()`, — иначе поле в «Экономике» пустое (грабля из трека В).
  Биллинг-тикер ожидающие сессии не трогает ВООБЩЕ: ни денег, ни минутного
  запаса, ни предупреждений; по истечении дедлайна подтверждает сам (Р1) и
  ставит точку отсчёта РОВНО на дедлайн, а не на момент тика — опоздание
  тикера (до 30 с) не должно доставаться ни гостю, ни клубу.
  **Точка отсчёта:** `POST /me/sessions/ready` сдвигает `started_at` на момент
  нажатия. Сдвиг одного поля честнее, чем учить каждого потребителя времени
  (биллинг, XP, пауза, AFK, ачивки, прогноз «до HH:MM») вычитать ожидание.
  Нажатие идемпотентно: двойной клик и перезагрузка экрана не сдвигают точку
  дважды. Дедлайн чужой брони (Г3-и2) считается от времени САМОЙ брони и на
  `started_at` не смотрит — ожидание не съедает чужое время; закреплено
  отдельным юнитом, потому что ошибка здесь тихо обкрадывает третье лицо.
  Сюда же взят серверный кусок и4: `POST /admin/sessions/:id/seat-cancel` —
  отмена посадки до нажатия. Это НЕ завершение: статус `cancelled` (уже был в
  enum), минут ноль, XP и монет нет — иначе «гость передумал у стойки» попал
  бы в статистику сыгранной сессией. Гонка с нажатием закрыта условием
  `ready_at IS NULL` прямо в UPDATE.
- [ ] и3. Шелл: полноэкранный экран «Готов?» — ник, «ПК-07», обратный отсчёт,
  крупная кнопка; лаунчер и вкладки заперты, пока не нажато. Строки в трёх
  словарях SH_RU/SH_EN/SH_PL.
- [ ] и4. Админка: «⏳ ждёт 6:12» на клетке зала и плитке ПК, в шторке —
  «Отменить посадку» (сессия отменяется без списания, ПК свободен, аудит
  `seat_cancel`); live-событие в ленту и на плитку.
- [ ] и5. Новый гость: «Занять под регистрацию» — ПК придержан на
  `ready_wait_min`, помечен в зале, не перехватывается; после регистрации
  гость попадает сразу в окно [Готов!]. Отмена придержания вручную.
- [ ] и6. PWA: то же окно подтверждения (гость может нажать с телефона),
  APP_VER + app_version.
- Критерий и1+и2: ✅ 2026-08-24, `e2e_ready.py` — 30 проверок в РЕАЛЬНОМ
  времени (подменять время значило бы проверять не то, ради чего спринт):
  за 70 с ожидания не списано ни гроша и не начислено ни минуты; нажатие
  сдвинуло точку отсчёта ровно на длительность ожидания, повторное нажатие
  идемпотентно, после него оплачено ~70 с вместо ~140 и ровно 400 грошей;
  авто-старт по дедлайну сработал сам, посадку не снял, расхождение точки
  отсчёта с дедлайном 0 с, в счёт попало только время ПОСЛЕ дедлайна; отмена
  посадки освободила ПК, не тронула кошелёк, не начислила XP и монет, легла в
  аудит, повторная — 409; играющего отменить нельзя (409 already_playing);
  `ready_wait_min=0` полностью выключает окно. Юниты: дедлайн окна, признак
  ожидания, «дорога к компу не оплачивается» (120 грошей переплаты, которых
  теперь нет), «авто-старт считает от дедлайна, а не от тика», «дедлайн брони
  не зависит от подтверждения». Миграция 045 прогнана дважды — идемпотентна.
  Регрессия Е0: 128/128.
- Осталось в Е1: и3 (экран «Готов?» на шелле), и4 (вид в зале админки —
  серверная часть уже есть), и5 («Занять под регистрацию»), и6 (PWA).
